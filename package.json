{
  "name": "nisti-identificacao",
  "private": true,
  "version": "0.5.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3000",
    "build": "vite build",
    "test": "node --test test/*.test.js",
    "deploy": "npm run build && wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply nisti-identificacao --remote"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "vite": "^6.2.0",
    "wrangler": "^3.114.0"
  },
  "devDependencies": {}
}
