{
  "name": "nisti-identificacao",
  "private": true,
  "version": "0.5.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "node --test test/*.test.js",
    "audit:gtin": "node scripts/audit-gtin-load.mjs",
    "deploy": "npm run build && wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply nisti-identificacao --remote"
  },
  "dependencies": {
    "@zxing/browser": "0.2.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "vite": "^6.2.0",
    "wrangler": "^3.114.0"
  }
}
